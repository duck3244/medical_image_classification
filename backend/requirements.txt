# medical_image_classification
# 개인 학습 목적 - 진단·임상 사용 금지
# TensorFlow 2.10 (기존 환경 재사용) 기준

# 딥러닝 프레임워크 (이미 설치된 tensorflow-gpu 2.10.0 사용)
# tensorflow>=2.10,<2.11

# TF 2.10 호환 AdamW + X-ray 회전증강. 미설치 시 trainer는 Adam(weight_decay 없음)으로 폴백.
tensorflow-addons==0.18.0

# 데이터 처리
numpy>=1.21.0,<1.27.0
pandas>=1.3.0
scikit-learn>=1.0.0

# 이미지 처리
opencv-python>=4.5.0
pillow>=8.3.0
scikit-image>=0.18.0
imageio>=2.9.0

# 시각화
matplotlib>=3.5.0
seaborn>=0.11.0

# 유틸리티
tqdm>=4.62.0
requests>=2.25.0
urllib3>=1.26.0
psutil>=5.8.0

# Grad-CAM
tf-keras-vis>=0.8.0

# HuggingFace Hub (NIH 미러 다운로드용)
huggingface_hub>=0.16.0

# 선택: ISIC API 클라이언트 (없으면 requests로 폴백)
# isic-cli>=0.2.0

# Web API (단일 사용자 MVP 서비스)
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
python-multipart>=0.0.9
